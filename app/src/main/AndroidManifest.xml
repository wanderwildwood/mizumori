<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      No permissions at all. A level reads the accelerometer, and Android does not gate
      the motion sensors behind a permission, so there is nothing here to ask for and
      nothing to grant.

      Upstream declares WAKE_LOCK to keep the screen on. This does not need it: the same
      job is done by the FLAG_KEEP_SCREEN_ON window flag set in MainActivity, which
      requires no permission and which Android clears by itself when the window loses
      focus. An earlier version of this comment claimed keeping the screen on was
      pointless on E Ink, on the theory that the panel holds its image with the backlight
      off. That was wrong: the display really does go off and the phone locks, and a level
      you have to wake up is a level you have to move.
    -->
    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.App">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
